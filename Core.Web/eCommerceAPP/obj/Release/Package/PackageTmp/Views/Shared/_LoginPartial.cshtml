@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm"}))
    {
    @Html.AntiForgeryToken()


        <span class="icon-holder">@Html.ActionLink(User.Identity.GetUserName() + " | ", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage", Style = "color:#00963B" })</span>

        <span class="icon-holder"><a href="javascript:document.getElementById('logoutForm').submit()" style="color:#00963B">Salir</a></span>

    }

}
else
{
    @*<span class="icon-holder">@Html.ActionLink("Registro", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink", Style = "color:#6c757d" })</span>
    <span class="icon-holder"> | </span>*@
    <span class="icon-holder">@Html.ActionLink("Iniciar Sesión", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink", Style = "color:#6c757d" })</span>
}
